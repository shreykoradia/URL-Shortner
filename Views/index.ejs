<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortner </title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We" crossorigin="anonymous">
</head>
<body>
   
    <div class="container">
        <h1>Shrink Your URL here </h1>
        <form class="my-4  form-inline" action="/shortUrl" method="POST">
            <label class="sr-only" for="fullURL"></label>
            <input class="my-2 form-control col mr-2"type="url"placeholder="URL" name="fullURL" id="fullURL">
            <button class="btn btn-success"type="submit">Shrink</button>
        </form>
        <div class="table-responsive-sm">
            <table class="table table-striped table-dark">
                <thead>
                    <th>Full-URL</th>
                    <th>Short-URL</th>
                    <th>Click's</th>
                </thead>
                <tbody> 
                    <% shortUrls.forEach(shortUrl => { %>
                        <tr>
                          <td><a href="<%= shortUrl.full %>"><%= shortUrl.full %></a></td>
                          <td><a href="<%= shortUrl.short %>"><%= shortUrl.short %></a></td>
                          <td><%= shortUrl.clicks %></td>
                        </tr>
                      <% }) %>    
                </tbody>
            </table>
        </div>
        
    </div>   
</body>
</html>